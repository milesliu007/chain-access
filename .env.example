# chain-access 环境变量配置

# 必须设置 — 缺失时服务拒绝启动
JWT_SECRET=your-secret-key-change-this
INFURA_API_KEY=your-infura-api-key

# 可选
PORT=8080
ALLOWED_ORIGINS=http://localhost:8080
# HTTP_PROXY=http://127.0.0.1:7897  # 仅测试环境需要，线上环境不设置

# 多链支持说明：
# 内置支持 Ethereum Mainnet 和 Avalanche Fuji Testnet
# 新增链请在 api/config/chain.go 的 DefaultChains() 中添加配置
# 所有链共用同一个 INFURA_API_KEY

# 后台管理系统（可选，不设置则管理功能禁用）
# MYSQL_DSN=user:password@tcp(127.0.0.1:3306)/chain_access?parseTime=true
# ADMIN_NFT_CONTRACT=0xYourNFTContractAddress   # ERC721 合约地址（管理员门控）
# ADMIN_NFT_CHAIN_ID=1                           # ERC721 所在链 ID（1=Ethereum Mainnet）
